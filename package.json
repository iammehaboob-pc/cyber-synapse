{
  "name": "game.ik",
  "private": true,
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev -- --host 0.0.0.0\"",
    "install:all": "npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
